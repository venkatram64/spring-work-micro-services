spring:
  application:
    name: service-registry-in-eureka
  instance:
    instance-id: ${spring.application.name}:${server.port}
  config:  #getting properties form config server
    import: "optional:configserver:http://localhost:8088"
