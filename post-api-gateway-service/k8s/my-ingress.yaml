apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-post-ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: post-api-gateway-service
                port:
                  number: 8060